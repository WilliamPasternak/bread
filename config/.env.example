# Server configuration
PORT=3000
SESSION_SECRET=your_session_secret_here

# MongoDB (replace <username>, <password>, <cluster-url>, <dbname>)
DB_STRING=mongodb+srv://<username>:<password>@<cluster-url>/<dbname>?retryWrites=true&w=majority

# Cloudinary (for image uploads)
CLOUD_NAME=your_cloud_name
API_KEY=your_api_key
API_SECRET=your_api_secret

# Email (for password resets and notifications)
GMAIL_USER=your@gmail.com
GMAIL_PASS=your_16_character_app_password

# Old nodemailer setup, might not be needed anymore. Will delete.
MAIL_USER=you@example.com
MAIL_PASS=your_email_password